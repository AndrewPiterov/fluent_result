name: fluent_result
description: A new Flutter package project.
version: 1.0.0
homepage: https://github.com/AndrewPiterov/fluent_result

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
